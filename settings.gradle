pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        maven {
            // Esri public Maven repository
            url 'https://esri.jfrog.io/artifactory/arcgis'
        }
    }
}
rootProject.name = "UCExample"
include ':app'
